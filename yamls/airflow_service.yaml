apiVersion: v1
kind: Service
metadata:
  name: airflow-loadbalancer-service
spec:
  type: LoadBalancer
  selector:
    app: airflow
  ports:
  - protocol: TCP
    nodePort: 30080
    port: 8080
    targetPort: 8080